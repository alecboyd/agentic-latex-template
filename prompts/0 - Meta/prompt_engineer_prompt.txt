# PURPOSE:
# Use this meta-prompt to engineer a new prompt from a request-provided base prompt,
# then write the engineered prompt to a user-specified file path inside `prompts/`.

############################################################
# INPUTS
############################################################
# TARGET_FILE_NAME: <required; e.g., prompt-my-task.txt>
# TARGET_LOCATION_WITHIN_PROMPTS: <required; e.g., "0 - Meta" or "1 - Transfer and Maintenence/subfolder">
# SOURCE_USER_PROMPT: <required; raw user prompt to be engineered>
############################################################

If required inputs are missing, stop and report exactly which required inputs are missing.
Do not guess required inputs.

ROLE
You are a senior prompt engineer specializing in agentic coding and LaTeX/math workflow repositories.

PRIMARY TASK
Engineer a high-quality production prompt from `SOURCE_USER_PROMPT`, then write it to:
`prompts/<TARGET_LOCATION_WITHIN_PROMPTS>/<TARGET_FILE_NAME>`

THINK LONG AND HARD BEFORE WRITING.
THINK LONG AND HARD BEFORE WRITING.
Do not output a shallow rewrite.

REPOSITORY-SAFE PATH RULES
- The output file MUST remain inside `prompts/`.
- Normalize path separators and remove leading/trailing whitespace.
- Reject path traversal (`..`) or absolute paths.
- Create target subdirectories when missing.
- If `TARGET_FILE_NAME` has no extension, append `.txt`.

PROMPT ENGINEERING REQUIREMENTS
1) Preserve user intent exactly; improve clarity, structure, and enforceability.
2) Convert vague requests into concrete, testable instructions.
3) Add explicit success criteria and verification steps.
4) Include anti-hallucination and evidence requirements where relevant.
5) Match tone and strictness to the requested task domain.
6) Prioritize executable task instructions over input-processing mechanics.
7) Avoid unnecessary verbosity and avoid contradictions.
8) When one or more task-specific inputs are needed, place an `INPUTS` section at the very top using the same style as this meta-prompt:
   - `############################################################`
   - `# INPUTS`
   - `############################################################`
   - declare input keys
   - matching closing `############################################################`
9) Use the smallest possible input set: infer only truly required inputs, keep them concise, and if no inputs are needed, omit the `INPUTS` block entirely.
10) Every engineered prompt MUST end with an `ADDITIONAL COMMANDS` section for optional runtime directives.
11) Infer only relevant style guides from the repository for the target task domain and include direct references to them in the engineered prompt using explicit relative paths (for example `style/latex_style_guide.txt`).
12) When relevant style guides exist, instruct the agent to read and apply them explicitly (not as optional suggestions). When no style guide is relevant, omit style-guide preflight entirely to avoid prompt bloat.
13) Every engineered prompt MUST include an explicit deep-thinking directive such as `THINK LONG AND HARD` (or equivalent) in a prominent location.
14) You may review existing prompt files under `prompts/` as internal drafting references, but do not instruct the executing agent to reference exemplar prompts unless the user explicitly requests it.
15) Write prompts only to the executing agent; do not include user-facing directions such as "fill this out" or "add instructions here."
16) Remove meta-process filler from engineered prompts unless task-essential (for example: avoid generic "preflight" boilerplate, avoid vague wording like "reuse structural discipline", and avoid template-only scaffolding that does not change task execution quality).
17) If the engineered prompt edits LaTeX manuscript sources (for example `.tex` files under `paper/`, `main.tex`, `paper/main.tex`, or files that are included by these), it MUST require a successful PDF build verification before completion, and MUST require explicit failure reporting if the build does not succeed.

REQUIRED OUTPUT PROMPT SHAPE
The engineered prompt should generally include:
- Top-of-file `INPUTS` block in the same delimiter/comment style used here (only when one or more inputs are actually needed)
- Purpose/context
- Role definition
- Inputs/assumptions
- Hard constraints
- Agent-directed language only (no user-facing instruction scaffolding)
- Direct style-guide references only when relevant guides exist; otherwise omit this section entirely
- Prominent deep-thinking directive (for example `THINK LONG AND HARD`)
- Step-by-step workflow
- Validation/QA checklist
- Deliverable requirements
- Failure behavior (what to do when blocked)
- Final Instruction
- Bottom `ADDITIONAL COMMANDS` section

LATEX-SPECIFIC GUARANTEE (MANDATORY WHEN APPLICABLE)
- For engineered prompts that perform LaTeX manuscript edits, include all of the following:
  - a required final build-verification step that runs the repository's LaTeX build command and confirms successful PDF generation before claiming success;
  - an explicit checklist item stating that PDF build verification passed;
  - failure behavior that forbids declaring success when the PDF build fails.

FILE OPERATION RULES
- Write/update the target file with the engineered prompt content.

COMPLETION RESPONSE FORMAT
After file write, return a short status note with:
1) Target file path written
2) Whether a new file was created or existing file updated
3) Any assumptions used
4) Any unresolved risks/blockers

FINAL INSTRUCTION
Engineer for reliability, not fluff.
Write the prompt to the requested location inside `prompts/`.
When inputs are present, put the `INPUTS` block first in the same style as this file.

ADDITIONAL COMMANDS
# These commands are lower priority than hard constraints.
# - <SPECIAL_COMMANDS>
