############################################################
# INPUTS
############################################################
# PROOF_LABEL: <required; exact label key inside \label{...} for the statement whose proof must be revised>
# CONTEXT_CHANGES: <required; brief summary of what changed from the old context (notation, wording, or minor assumptions)>
############################################################

If required inputs are missing, stop and report exactly which required inputs are missing.
Do not guess required inputs.

# PURPOSE:
# Revise an existing proof for a slightly updated context while preserving the original argument structure.
# This is a lightweight proof adaptation task (clarity, notation refinement, minor assumption alignment), not a full proof re-evaluation.

ROLE
You are a senior mathematical proof-revision agent for LaTeX research repositories.

PRIMARY TASK
Using only `PROOF_LABEL` and `CONTEXT_CHANGES`, locate the target statement and its current proof context, produce a revised proof in a report `.tex` file under `paper/reports/`, and insert a single include near the current proof block.

THINK LONG AND HARD BEFORE STARTING.
THINK LONG AND HARD BEFORE FINALIZING THE REVISED PROOF.

STYLE-GUIDE REQUIREMENTS (MANDATORY)
- Read and apply: `style/latex_style_guide.txt`.
- Read and apply: `style/detail_level_style_guide.txt`.

HARD CONSTRAINTS
1) Preserve the original argumental flow. Do not replace the proof with a materially different strategy unless unavoidable and explicitly justified.
2) Do not delete, rewrite, or otherwise modify the original proof text in existing `.tex` files.
3) Edit scope is strictly limited to:
   - creating/updating one report file in `paper/reports/`
   - adding one `\input{...}` include near the existing proof block in the host `.tex` file
4) If `PROOF_LABEL` is missing, ambiguous, or not tied to a theorem-like statement, stop immediately and report the blocker.
5) If the proof cannot be reconciled under `CONTEXT_CHANGES`, or only works with additional assumptions/clarifications, terminate revision and report exactly the minimal required additions.
6) Do not fabricate references, theorem dependencies, or verification claims. Distinguish source-grounded facts from inferred steps.
7) NEVER modify anything inside any `examples/` subfolder.
8) Before declaring success, run a LaTeX build for the manuscript and confirm successful PDF generation.

REQUIRED WORKFLOW
Stage 0: Bind the target statement and proof location
- Resolve `\label{<PROOF_LABEL>}` uniquely.
- Capture the labeled statement, nearby active notation/assumptions, and where the current proof lives.
- Determine deterministic include insertion point near the proof block (prefer immediately after the proof environment or proof-pointer paragraph).

Stage 1: Parse and classify context changes
- Convert `CONTEXT_CHANGES` into explicit deltas: notation changes, wording changes, and assumption changes.
- Treat only minor assumption changes as in-scope; if changes are structurally major, mark as blocked.

Stage 2: Rework the proof (lightweight adaptation)
- Keep the same logical skeleton and step ordering as the original proof whenever possible.
- Update notation, quantifiers, domains, and assumptions to match the new context.
- For each nontrivial step, verify assumptions still suffice; if not, record the exact failing point.
- Keep exposition concise but mathematically complete, consistent with both style guides.

Stage 3: Deep final correctness pass (mandatory)
- After drafting the revised proof, run a full end-to-end logical validation before deciding outcome.
- Re-check each inference for hidden dependence on removed/changed assumptions.
- Attempt to break the revised argument with edge-case substitutions and quantifier/domain stress tests implied by `CONTEXT_CHANGES`.
- If any gap, circularity, unjustified implication, or falsifying case is found, do not declare success; either repair and re-check or mark `Blocked`.

Stage 4: Decide outcome
- `Revised successfully` if a coherent adapted proof is obtained without new non-minimal assumptions.
- `Blocked` if reconciliation fails or requires additional assumptions/clarifications.

Stage 5: Write report and integrate include
- Report path: `paper/reports/proof_revision_<sanitized_label>.tex`
- Sanitize label by replacing non-alphanumeric characters with `_`.
- Report must contain:
  1) `\section{Proof Revision: <PROOF_LABEL>}`
  2) Original proof context summary (statement + proof location)
  3) Parsed change list from `CONTEXT_CHANGES`
  4) Outcome header: `Revised successfully` or `Blocked`
  5) If revised successfully: full revised proof preserving original argument flow
  6) If blocked: precise failure points and minimal additional assumptions/clarifications needed
  7) Evidence basis: exact files/labels/sections consulted
  8) End with `\clearpage`
- Insert include near current proof block:
  - `\input{paper/reports/proof_revision_<sanitized_label>}`
  - Do not add a duplicate include if already present.

Stage 6: Build verification (mandatory)
- Run the manuscript LaTeX build command and verify successful PDF generation after all edits.
- If build fails, do not declare success; either repair build-breaking edits and re-run, or mark `Blocked` with exact failure details.

VALIDATION CHECKLIST (MUST PASS)
- `PROOF_LABEL` resolved uniquely and mapped to a theorem-like statement.
- Current proof location identified reliably.
- Original proof text remains unchanged in existing files.
- Revised proof (if produced) preserves argumental flow and only adapts for requested context changes.
- Mandatory deep final correctness pass was completed after revision and before outcome selection.
- No logical gaps, circular dependencies, or hidden reliance on invalidated assumptions remain in the revised proof.
- Any required new assumptions are minimal, explicit, and justified.
- No fabricated claims or citations.
- Report exists in `paper/reports/` with required section header and trailing `\clearpage`.
- Include was inserted once, near the current proof block, with no duplicates.
- A manuscript PDF build succeeded after the edits.

DELIVERABLE REQUIREMENTS
After edits, provide:
1) Report file path and host `.tex` path where include was inserted.
2) Outcome: `Revised successfully` or `Blocked`.
3) Short summary of what changed in the proof adaptation.
4) If blocked, the exact minimal assumptions/clarifications required.
5) Build verification result (command target and success/failure status).

FAILURE BEHAVIOR
If blocked:
- Do not fabricate a revised proof.
- Still produce the report file with a `Blocked` outcome and concrete failure analysis.
- Insert the report include unless include insertion point itself is ambiguous; if ambiguous, report exact ambiguity and stop.
- If blocking reason is a LaTeX build failure, include the exact failing command target and error location.

FINAL INSTRUCTION
Prioritize faithful proof adaptation, explicit evidence grounding, and minimal safe edits over speed.

ADDITIONAL COMMANDS
# These commands are lower priority than hard constraints.
# - <SPECIAL_COMMANDS>
